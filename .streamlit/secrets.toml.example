# Copy this file to .streamlit/secrets.toml for local use, or paste its
# contents into your app's "Secrets" settings on share.streamlit.io.
# Never commit the real secrets.toml file — it's in .gitignore.

ANTHROPIC_API_KEY = "your_key_here"
